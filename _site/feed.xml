<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://0.0.0.0:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://0.0.0.0:4000/" rel="alternate" type="text/html" /><updated>2026-01-27T07:44:51+00:00</updated><id>http://0.0.0.0:4000/feed.xml</id><title type="html">Thejaeu Dev-log</title><subtitle>Game Server Programmer</subtitle><author><name>김재우(thejaeu)</name></author><entry><title type="html">Sample</title><link href="http://0.0.0.0:4000/test/sample/" rel="alternate" type="text/html" title="Sample" /><published>2026-01-01T00:00:00+00:00</published><updated>2026-01-01T00:00:00+00:00</updated><id>http://0.0.0.0:4000/test/sample</id><content type="html" xml:base="http://0.0.0.0:4000/test/sample/"><![CDATA[<ul>
  <li>Sample Post</li>
</ul>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"hello world!</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>]]></content><author><name>김재우(thejaeu)</name></author><category term="Test" /><category term="Test" /><category term="First" /><summary type="html"><![CDATA[Sample Post #include &lt;iostream&gt; int main() { std::cout &lt;&lt; "hello world!\n"; }]]></summary></entry><entry><title type="html">C++ random_device</title><link href="http://0.0.0.0:4000/c++/random-device/" rel="alternate" type="text/html" title="C++ random_device" /><published>2025-12-28T00:00:00+00:00</published><updated>2025-12-28T00:00:00+00:00</updated><id>http://0.0.0.0:4000/c++/random-device</id><content type="html" xml:base="http://0.0.0.0:4000/c++/random-device/"><![CDATA[<blockquote>
  <p>오류 사항은 contact@thejaeu.com 으로 보내주시기 바랍니다</p>
</blockquote>

<h2 id="기본-사용">기본 사용</h2>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;random&gt;</span><span class="cp">
</span>
<span class="c1">// ...</span>

<span class="n">std</span><span class="o">::</span><span class="n">random_device</span> <span class="n">seed</span><span class="p">;</span>
<span class="n">std</span><span class="o">::</span><span class="n">mt19937</span> <span class="nf">generator</span><span class="p">(</span><span class="n">seed</span><span class="p">());</span>

<span class="k">auto</span> <span class="n">rNumber</span> <span class="o">=</span> <span class="n">generator</span><span class="p">();</span>

<span class="c1">// ...</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">&lt;random&gt;</code> 헤더를 통해 random_device를 초기화 하고 mt19937(유사 난수 생성기)를 해당 random_device로 초기화 함</li>
  <li>mt19937에는 <code class="language-plaintext highlighter-rouge">operator()</code>가 정의 되어있으며 호출만 하면 난수가 생성이 됨</li>
</ul>

<h2 id="random-number-distribution">random number distribution</h2>

<h3 id="정수-균등-분포-생성">정수 균등 분포 생성</h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;random&gt;</span><span class="cp">
</span>
<span class="c1">// ...</span>

<span class="n">std</span><span class="o">::</span><span class="n">random_device</span> <span class="n">seed</span><span class="p">;</span>
<span class="n">std</span><span class="o">::</span><span class="n">mt19937</span> <span class="nf">generator</span><span class="p">(</span><span class="n">seed</span><span class="p">());</span>
<span class="n">std</span><span class="o">::</span><span class="n">uniform_int_distribution</span><span class="o">&lt;&gt;</span> <span class="n">dis</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">100</span><span class="p">);</span> <span class="c1">// inclusive 0 and 100</span>

<span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">i</span><span class="o">=</span><span class="mi">0</span><span class="p">;</span><span class="n">i</span><span class="o">&lt;</span><span class="mi">100</span><span class="p">;</span><span class="o">++</span><span class="n">i</span><span class="p">)</span>
	<span class="k">auto</span> <span class="n">num</span> <span class="o">=</span> <span class="n">dis</span><span class="p">(</span><span class="n">gen</span><span class="p">);</span> <span class="c1">// 0부터 100까지 균등한 확률로 생성됨</span>

<span class="c1">// ...</span>

</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">uniform_int_distribution</code>을 통해 균등 분포된 값을 생성할 수 있음</li>
</ul>

<h3 id="정수-정규-분포-생성">정수 정규 분포 생성</h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">```</span><span class="n">cpp</span>
<span class="cp">#include</span> <span class="cpf">&lt;random&gt;</span><span class="cp">
</span>
<span class="c1">// ...</span>

<span class="n">std</span><span class="o">::</span><span class="n">random_device</span> <span class="n">seed</span><span class="p">;</span>
<span class="n">std</span><span class="o">::</span><span class="n">mt19937</span> <span class="nf">generator</span><span class="p">(</span><span class="n">seed</span><span class="p">());</span>
<span class="n">std</span><span class="o">::</span><span class="n">normal_distribution</span><span class="o">&lt;&gt;</span> <span class="n">dis</span><span class="p">(</span><span class="mi">50</span><span class="p">,</span> <span class="mi">10</span><span class="p">);</span> <span class="c1">// mean 50, stddev 10</span>

<span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">i</span><span class="o">=</span><span class="mi">0</span><span class="p">;</span><span class="n">i</span><span class="o">&lt;</span><span class="mi">100</span><span class="p">;</span><span class="o">++</span><span class="n">i</span><span class="p">)</span>
	<span class="k">auto</span> <span class="n">num</span> <span class="o">=</span> <span class="n">dis</span><span class="p">(</span><span class="n">gen</span><span class="p">);</span> <span class="c1">// 100 개의 난수가 평균 50이 나오도록 표준편차 10으로 생성됨</span>

<span class="c1">// ...</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">normal_distribution</code>을 통해 정규 분포된 값을 생성할 수 있음</li>
</ul>

<h3 id="실수-균등-분포-생성">실수 균등 분포 생성</h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;random&gt;</span><span class="cp">
</span>
<span class="c1">// ...</span>

<span class="n">std</span><span class="o">::</span><span class="n">random_device</span> <span class="n">seed</span><span class="p">;</span>
<span class="n">std</span><span class="o">::</span><span class="n">mt19937</span> <span class="nf">generator</span><span class="p">(</span><span class="n">seed</span><span class="p">());</span>
<span class="n">std</span><span class="o">::</span><span class="n">uniform_real_distribution</span><span class="o">&lt;&gt;</span> <span class="n">dis</span><span class="p">(</span><span class="o">-</span><span class="mi">100</span><span class="p">,</span> <span class="mi">100</span><span class="p">);</span> <span class="c1">// inclusive -100 and 100</span>

<span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">i</span><span class="o">=</span><span class="mi">0</span><span class="p">;</span><span class="n">i</span><span class="o">&lt;</span><span class="mi">100</span><span class="p">;</span><span class="o">++</span><span class="n">i</span><span class="p">)</span>
	<span class="k">auto</span> <span class="n">num</span> <span class="o">=</span> <span class="n">dis</span><span class="p">(</span><span class="n">gen</span><span class="p">);</span> <span class="c1">// double 타입의 값을 return 함</span>
<span class="c1">// ...</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">uniform_real_distribution</code>을 통해 균등 분포 된 실수 값을 생성할 수 있음</li>
</ul>

<h3 id="실수-정규-분포-생성">실수 정규 분포 생성</h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;random&gt;</span><span class="cp">
</span>
<span class="c1">// ...</span>

<span class="n">std</span><span class="o">::</span><span class="n">random_device</span> <span class="n">seed</span><span class="p">;</span>
<span class="n">std</span><span class="o">::</span><span class="n">mt19937</span> <span class="nf">generator</span><span class="p">(</span><span class="n">seed</span><span class="p">());</span>
<span class="n">std</span><span class="o">::</span><span class="n">normal_distribution</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span> <span class="n">dis</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">8</span><span class="p">);</span> <span class="c1">// mean 0, stddev 8</span>

<span class="k">for</span><span class="p">(</span><span class="kt">int</span> <span class="n">i</span><span class="o">=</span><span class="mi">0</span><span class="p">;</span><span class="n">i</span><span class="o">&lt;</span><span class="mi">100</span><span class="p">;</span><span class="o">++</span><span class="n">i</span><span class="p">)</span>
	<span class="k">auto</span> <span class="n">num</span> <span class="o">=</span> <span class="n">dis</span><span class="p">(</span><span class="n">gen</span><span class="p">);</span> <span class="c1">// double 타입의 값을 return 함</span>
<span class="c1">// ...</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">normal_distribution</code>의 타입 명시를 통해 정규 분포된 실수 값을 생성할 수 있음</li>
</ul>]]></content><author><name>김재우(thejaeu)</name></author><category term="C++" /><category term="c++" /><category term="cpp" /><category term="random_device" /><category term="mt19937" /><summary type="html"><![CDATA[오류 사항은 contact@thejaeu.com 으로 보내주시기 바랍니다 기본 사용 #include &lt;random&gt; // ... std::random_device seed; std::mt19937 generator(seed()); auto rNumber = generator(); // ... &lt;random&gt; 헤더를 통해 random_device를 초기화 하고 mt19937(유사 난수 생성기)를 해당 random_device로 초기화 함 mt19937에는 operator()가 정의 되어있으며 호출만 하면 난수가 생성이 됨 random number distribution 정수 균등 분포 생성 #include &lt;random&gt; // ... std::random_device seed; std::mt19937 generator(seed()); std::uniform_int_distribution&lt;&gt; dis(0, 100); // inclusive 0 and 100 for(int i=0;i&lt;100;++i) auto num = dis(gen); // 0부터 100까지 균등한 확률로 생성됨 // ... uniform_int_distribution을 통해 균등 분포된 값을 생성할 수 있음 정수 정규 분포 생성 ```cpp #include &lt;random&gt; // ... std::random_device seed; std::mt19937 generator(seed()); std::normal_distribution&lt;&gt; dis(50, 10); // mean 50, stddev 10 for(int i=0;i&lt;100;++i) auto num = dis(gen); // 100 개의 난수가 평균 50이 나오도록 표준편차 10으로 생성됨 // ... normal_distribution을 통해 정규 분포된 값을 생성할 수 있음 실수 균등 분포 생성 #include &lt;random&gt; // ... std::random_device seed; std::mt19937 generator(seed()); std::uniform_real_distribution&lt;&gt; dis(-100, 100); // inclusive -100 and 100 for(int i=0;i&lt;100;++i) auto num = dis(gen); // double 타입의 값을 return 함 // ... uniform_real_distribution을 통해 균등 분포 된 실수 값을 생성할 수 있음 실수 정규 분포 생성 #include &lt;random&gt; // ... std::random_device seed; std::mt19937 generator(seed()); std::normal_distribution&lt;double&gt; dis(0, 8); // mean 0, stddev 8 for(int i=0;i&lt;100;++i) auto num = dis(gen); // double 타입의 값을 return 함 // ... normal_distribution의 타입 명시를 통해 정규 분포된 실수 값을 생성할 수 있음]]></summary></entry><entry><title type="html">스마트 포인터 순환 참조 문제</title><link href="http://0.0.0.0:4000/c++/smart-pointer/" rel="alternate" type="text/html" title="스마트 포인터 순환 참조 문제" /><published>2025-12-27T00:00:00+00:00</published><updated>2025-12-27T00:00:00+00:00</updated><id>http://0.0.0.0:4000/c++/smart-pointer</id><content type="html" xml:base="http://0.0.0.0:4000/c++/smart-pointer/"><![CDATA[<blockquote>
  <p>오류 사항은 contact@thejaeu.com 으로 보내주시기 바랍니다</p>
</blockquote>

<ul>
  <li>개인 프로젝트를 진행하면서 안전한 포인터 사용을 위해 스마트 포인터를 사용하였지만 사용 미숙으로 인해 <code class="language-plaintext highlighter-rouge">shared_ptr</code>의 상호 참조 문제가 발생하였다</li>
  <li>메모리가 해제되지 못하고 끝까지 남아있는 것을 발견하고 해당 문제를 해결하게 되었다</li>
</ul>

<h2 id="문제점">문제점</h2>

<ul>
  <li>shared_ptr은 하나의 메모리를 여러 객체가 공유하고자 할 때 사용할 수 있음</li>
  <li>당연하게도 <code class="language-plaintext highlighter-rouge">A</code>라는 클래스에 대한 shared_ptr을 <code class="language-plaintext highlighter-rouge">B</code>라는 클래스의 객체가 소유할 수 있음</li>
</ul>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Class</span> <span class="n">A</span>
<span class="p">{</span>
<span class="nl">public:</span>
	<span class="n">A</span><span class="p">()</span> <span class="p">{}</span>
	<span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">B</span><span class="o">&gt;</span> <span class="n">b</span><span class="p">;</span>
<span class="p">};</span>

<span class="n">Class</span> <span class="n">B</span>
<span class="p">{</span>
<span class="nl">public:</span>
	<span class="n">B</span><span class="p">()</span> <span class="p">{}</span>
	<span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">A</span><span class="o">&gt;</span> <span class="n">a</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="n">main</span><span class="p">()</span>
<span class="p">{</span>
	<span class="k">auto</span> <span class="n">a</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">A</span><span class="o">&gt;</span><span class="p">();</span>
	<span class="k">auto</span> <span class="n">b</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">B</span><span class="o">&gt;</span><span class="p">();</span>

	<span class="c1">// 서로 참조 횟수 증가</span>
	<span class="n">a</span><span class="o">-&gt;</span><span class="n">b</span> <span class="o">=</span> <span class="n">b</span><span class="p">;</span>
	<span class="n">b</span><span class="o">-&gt;</span><span class="n">a</span> <span class="o">=</span> <span class="n">a</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<ul>
  <li>위와 같은 코드는 shared_ptr의 잘못된 사용의 예시임</li>
  <li>A 가 B 에 대한 shared_ptr을 소유하고 B 가 A 에 대한 shared_ptr을 소유하는 순환 참조 문제가 일어남</li>
  <li>shared_ptr은 참조 카운트를 내부적으로 가지고 있으며 참조 카운트가 0이 될 경우 소멸하는 로직을 가지고있음</li>
  <li>결론적으로 객체가 소멸되어야 할 타이밍에 소멸되지 못하고 메모리 누수가 발생함</li>
</ul>

<h2 id="해결-방법">해결 방법</h2>

<ul>
  <li>순환 참조가 일어날 가능성이 있는 구조에서는 <code class="language-plaintext highlighter-rouge">weak_ptr</code>을 사용하여 소유권을 가지지 않도록 해야함</li>
</ul>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">A</span>
<span class="p">{</span>
<span class="nl">public:</span>
	<span class="n">A</span><span class="p">()</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Created A</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span> <span class="p">}</span>
	<span class="o">~</span><span class="n">A</span><span class="p">()</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Destroyed A</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span> <span class="p">}</span>
	<span class="n">std</span><span class="o">::</span><span class="n">weak_ptr</span><span class="o">&lt;</span><span class="n">B</span><span class="o">&gt;</span> <span class="n">b</span><span class="p">;</span>
<span class="p">};</span>

<span class="k">class</span> <span class="nc">B</span>
<span class="p">{</span>
<span class="nl">public:</span>
	<span class="n">B</span><span class="p">()</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Created B</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span> <span class="p">}</span>
	<span class="o">~</span><span class="n">B</span><span class="p">()</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Destoryed B</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span> <span class="p">}</span>
	<span class="n">std</span><span class="o">::</span><span class="n">weak_ptr</span><span class="o">&lt;</span><span class="n">A</span><span class="o">&gt;</span> <span class="n">a</span><span class="p">;</span>
<span class="p">};</span>

<span class="kt">int</span> <span class="n">main</span><span class="p">()</span>
<span class="p">{</span>
	<span class="k">auto</span> <span class="n">a</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">A</span><span class="o">&gt;</span><span class="p">();</span>
	<span class="k">auto</span> <span class="n">b</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">B</span><span class="o">&gt;</span><span class="p">();</span>

	<span class="n">a</span><span class="o">-&gt;</span><span class="n">b</span> <span class="o">=</span> <span class="n">b</span><span class="p">;</span>
	<span class="n">b</span><span class="o">-&gt;</span><span class="n">a</span> <span class="o">=</span> <span class="n">a</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<ul>
  <li>weak_ptr로 공유를 받게 될 경우 참조 카운트를 증가시키지 않고 shared_ptr의 weak 카운트를 증가시키며 이는 객체의 소멸 조건에 영향을 주지 않음
    <ul>
      <li>결론적으로 참조 카운트는 0이 되며 안전하게 소멸자를 호출함</li>
    </ul>
  </li>
</ul>

<h2 id="shared_ptr의-구조">shared_ptr의 구조</h2>

<ul>
  <li>shared_ptr은 메모리 블록에 실제 데이터 메모리 영역과 이를 관리하는 컨트롤 블록(Control Block)영역으로 나뉨</li>
  <li>이때 컨트롤 블록은 참조 카운트, weak 카운트 (약한 참조), 객체 소멸자 포인터 등 해당 메모리를 관리하기 위한 정보들로 이루어져있음</li>
  <li>참조 카운트가 0이 되면 객체는 소멸함, 그러나 weak 카운트가 0이 아니라면 컨트롤 블록은 소멸하지 않음
    <ul>
      <li>소멸 체크를 위한 로직</li>
    </ul>
  </li>
</ul>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">auto</span> <span class="n">a</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">A</span><span class="o">&gt;</span><span class="p">();</span>

<span class="c1">// 여기서 자동으로 약한 참조</span>
<span class="n">std</span><span class="o">::</span><span class="n">weak_ptr</span><span class="o">&lt;</span><span class="n">A</span><span class="o">&gt;</span> <span class="n">weakA</span> <span class="o">=</span> <span class="n">a</span><span class="p">;</span>
<span class="k">if</span><span class="p">(</span><span class="k">auto</span> <span class="n">sharedA</span> <span class="o">=</span> <span class="n">weakA</span><span class="p">.</span><span class="n">lock</span><span class="p">())</span>
<span class="p">{</span>
	<span class="n">sharedA</span><span class="o">-&gt;</span><span class="n">DoSomething</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div></div>

<ul>
  <li>위와 같은 방식으로 shared_ptr로 초기화 된 a 객체에 대해서 약한 참조를 할 때 <code class="language-plaintext highlighter-rouge">lock()</code>을 통해 객체가 유효한지 확인 할 수 있으며 <code class="language-plaintext highlighter-rouge">lock()</code>은 shared_ptr을 반환함</li>
</ul>]]></content><author><name>김재우(thejaeu)</name></author><category term="C++" /><category term="C++" /><category term="스마트포인터" /><category term="memory" /><category term="순환참조" /><summary type="html"><![CDATA[오류 사항은 contact@thejaeu.com 으로 보내주시기 바랍니다 개인 프로젝트를 진행하면서 안전한 포인터 사용을 위해 스마트 포인터를 사용하였지만 사용 미숙으로 인해 shared_ptr의 상호 참조 문제가 발생하였다 메모리가 해제되지 못하고 끝까지 남아있는 것을 발견하고 해당 문제를 해결하게 되었다 문제점 shared_ptr은 하나의 메모리를 여러 객체가 공유하고자 할 때 사용할 수 있음 당연하게도 A라는 클래스에 대한 shared_ptr을 B라는 클래스의 객체가 소유할 수 있음 Class A { public: A() {} std::shared_ptr&lt;B&gt; b; }; Class B { public: B() {} std::shared_ptr&lt;A&gt; a; } int main() { auto a = std::make_shared&lt;A&gt;(); auto b = std::make_shared&lt;B&gt;(); // 서로 참조 횟수 증가 a-&gt;b = b; b-&gt;a = a; } 위와 같은 코드는 shared_ptr의 잘못된 사용의 예시임 A 가 B 에 대한 shared_ptr을 소유하고 B 가 A 에 대한 shared_ptr을 소유하는 순환 참조 문제가 일어남 shared_ptr은 참조 카운트를 내부적으로 가지고 있으며 참조 카운트가 0이 될 경우 소멸하는 로직을 가지고있음 결론적으로 객체가 소멸되어야 할 타이밍에 소멸되지 못하고 메모리 누수가 발생함 해결 방법 순환 참조가 일어날 가능성이 있는 구조에서는 weak_ptr을 사용하여 소유권을 가지지 않도록 해야함 class A { public: A() { std::cout &lt;&lt; "Created A\n"; } ~A() { std::cout &lt;&lt; "Destroyed A\n"; } std::weak_ptr&lt;B&gt; b; }; class B { public: B() { std::cout &lt;&lt; "Created B\n"; } ~B() { std::cout &lt;&lt; "Destoryed B\n"; } std::weak_ptr&lt;A&gt; a; }; int main() { auto a = std::make_shared&lt;A&gt;(); auto b = std::make_shared&lt;B&gt;(); a-&gt;b = b; b-&gt;a = a; } weak_ptr로 공유를 받게 될 경우 참조 카운트를 증가시키지 않고 shared_ptr의 weak 카운트를 증가시키며 이는 객체의 소멸 조건에 영향을 주지 않음 결론적으로 참조 카운트는 0이 되며 안전하게 소멸자를 호출함 shared_ptr의 구조 shared_ptr은 메모리 블록에 실제 데이터 메모리 영역과 이를 관리하는 컨트롤 블록(Control Block)영역으로 나뉨 이때 컨트롤 블록은 참조 카운트, weak 카운트 (약한 참조), 객체 소멸자 포인터 등 해당 메모리를 관리하기 위한 정보들로 이루어져있음 참조 카운트가 0이 되면 객체는 소멸함, 그러나 weak 카운트가 0이 아니라면 컨트롤 블록은 소멸하지 않음 소멸 체크를 위한 로직 auto a = std::make_shared&lt;A&gt;(); // 여기서 자동으로 약한 참조 std::weak_ptr&lt;A&gt; weakA = a; if(auto sharedA = weakA.lock()) { sharedA-&gt;DoSomething(); } 위와 같은 방식으로 shared_ptr로 초기화 된 a 객체에 대해서 약한 참조를 할 때 lock()을 통해 객체가 유효한지 확인 할 수 있으며 lock()은 shared_ptr을 반환함]]></summary></entry><entry><title type="html">Boost.asio async 사용 및 주의점</title><link href="http://0.0.0.0:4000/c++/asio-warning/" rel="alternate" type="text/html" title="Boost.asio async 사용 및 주의점" /><published>2024-12-06T00:00:00+00:00</published><updated>2024-12-06T00:00:00+00:00</updated><id>http://0.0.0.0:4000/c++/asio-warning</id><content type="html" xml:base="http://0.0.0.0:4000/c++/asio-warning/"><![CDATA[<h2 id="boostasio-async_write">Boost.Asio async_write</h2>

<ul>
  <li>
    <p>Boost.Asio 라이브러리의 async_write를 사용하는 방법은 다음과 같음</p>

    <blockquote>
      <p><code class="language-plaintext highlighter-rouge">boost::asio::async_write(s, boost::asio::buffer(data, size), handler);</code></p>
    </blockquote>
  </li>
  <li>각 Parameter는 <code class="language-plaintext highlighter-rouge">AsyncWirteStream</code>, <code class="language-plaintext highlighter-rouge">ConstBufferSequence</code>, <code class="language-plaintext highlighter-rouge">WriteHandler</code>로 되어있음</li>
  <li>두 번째 Parameter에는 string과 같이 서버 혹은 클라이언트로 보낼 데이터가 들어가게 되며, 공식 Document에서는 Boost.Asio 라이브러리의 buffer를 사용하도록 권장함</li>
</ul>

<h2 id="example">Example</h2>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">write</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">message</span><span class="p">)</span>
<span class="p">{</span>
<span class="k">auto</span> <span class="n">self</span><span class="p">(</span><span class="n">shared_from_this</span><span class="p">());</span>

    <span class="n">boost</span><span class="o">::</span><span class="n">asio</span><span class="o">::</span><span class="n">async_write</span><span class="p">(</span><span class="n">_socket</span><span class="p">,</span> <span class="n">boost</span><span class="o">::</span><span class="n">asio</span><span class="o">::</span><span class="n">buffer</span><span class="p">(</span><span class="n">message</span><span class="p">),</span>
    <span class="p">[</span><span class="n">self</span><span class="p">,</span> <span class="n">message</span><span class="p">](</span><span class="n">boost</span><span class="o">::</span><span class="n">system</span><span class="o">::</span><span class="n">error_code</span> <span class="n">ec</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="kt">size_t</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="k">if</span><span class="p">(</span><span class="n">ec</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">cerr</span> <span class="o">&lt;&lt;</span> <span class="s">"Error: "</span> <span class="o">&lt;&lt;</span> <span class="n">ec</span><span class="p">.</span><span class="n">message</span><span class="p">()</span> <span class="o">&lt;&lt;</span> <span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
            <span class="k">return</span><span class="p">;</span>
        <span class="p">}</span>

        <span class="n">self</span><span class="o">-&gt;</span><span class="n">read</span><span class="p">();</span> <span class="c1">// 다음 데이터 전송을 대기하기 위한 호출</span>
    <span class="p">});</span>

<span class="p">}</span>
</code></pre></div></div>

<ul>
  <li>예시 코드에서는 Write를 호출하면서 매개변수로 shared_ptr<string>을 넘겨주게 되어있으며 async_write에 매개변수를 넘겨줄 때에는 message를 boost::asio::buffer를 통해 넘겨줌</string></li>
  <li>또한 Handler는 Lambda를 통해서 매개변수를 넘겨주었음</li>
</ul>

<h2 id="주의할-점">주의할 점</h2>

<ul>
  <li>Async 함수 내의 로컬 변수를 handler에서 사용할 가능성이 있다면 메모리 해제를 방지해야 함</li>
  <li>
    <p><strong>예제에서의 <code class="language-plaintext highlighter-rouge">message</code>에 대한 관리</strong></p>
  </li>
  <li>공식 문서에는 다음과 같이 설명되어 있음</li>
</ul>

<blockquote>
  <p>One or more buffers containing the data to be written. Although the buffers object may be copied as necessary, ownership of the underlying memory blocks is retained by the caller, which must guarantee that they remain valid until the handler is called.</p>
</blockquote>

<blockquote>
  <p>기록할 데이터가 포함된 하나 이상의 버퍼. 버퍼 개체는 필요에 따라 복사할 수 있지만 기본 메모리 블록의 소유권은 호출자가 유지하므로 핸들러가 호출될 때까지 유효하게 유지되어야 합니다. (papago 번역)</p>
</blockquote>

<ul>
  <li>쉽게 말하자면 예제에서의 message는 handler가 호출 될 때 까지 유효해야 한다는 것</li>
  <li>이와 같이 공식 문서에서도 메모리 관리에 대해 주의해야 한다고 되어있음</li>
</ul>

<h2 id="해결-방안">해결 방안</h2>

<ul>
  <li>스마트 포인터의 사용</li>
</ul>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">write</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="o">&gt;</span> <span class="n">message</span><span class="p">)</span>
<span class="p">{</span>
    <span class="k">auto</span> <span class="n">self</span><span class="p">(</span><span class="n">shared_from_this</span><span class="p">());</span>
    <span class="n">boost</span><span class="o">::</span><span class="n">asio</span><span class="o">::</span><span class="n">async_write</span><span class="p">(</span><span class="n">_socket</span><span class="p">,</span> <span class="n">boost</span><span class="o">::</span><span class="n">asio</span><span class="o">::</span><span class="n">buffer</span><span class="p">(</span><span class="o">*</span><span class="n">message</span><span class="p">),</span>
        <span class="p">[</span><span class="n">self</span><span class="p">,</span> <span class="n">message</span><span class="p">](</span><span class="n">boost</span><span class="o">::</span><span class="n">system</span><span class="o">::</span><span class="n">error_code</span> <span class="n">ec</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="kt">size_t</span><span class="p">)</span>
        <span class="p">{</span>
            <span class="k">if</span><span class="p">(</span><span class="n">ec</span><span class="p">)</span>
            <span class="p">{</span>
                <span class="n">std</span><span class="o">::</span><span class="n">cerr</span> <span class="o">&lt;&lt;</span> <span class="s">"Error: "</span> <span class="o">&lt;&lt;</span> <span class="n">ec</span><span class="p">.</span><span class="n">message</span><span class="p">()</span> <span class="o">&lt;&lt;</span> <span class="s">"</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
                <span class="k">return</span><span class="p">;</span>
            <span class="p">}</span>

       <span class="n">self</span><span class="o">-&gt;</span><span class="n">read</span><span class="p">();</span>
    <span class="p">});</span>
<span class="p">}</span>
</code></pre></div></div>

<ul>
  <li>message 자체를 shared_ptr 로 관리하며 참조 count를 통해 소멸 시점을 늦춤</li>
  <li>공식 문서에서 권장하는 방식과 부합하며 비동기적인 처리를 할 수 있는 가장 편하고 안전한 방법이라고 생각하여 사용하게 됨</li>
</ul>

<h2 id="결론">결론</h2>

<ul>
  <li>async_write() 와 같은 비동기 처리 로직이 있다면 스마트 포인터를 적극 활용하여 안전한 비동기 처리를 할 수 있도록 해야 함</li>
</ul>]]></content><author><name>김재우(thejaeu)</name></author><category term="C++" /><category term="C++" /><category term="boost.asio" /><category term="asio" /><category term="async" /><category term="dangling-pointer" /><summary type="html"><![CDATA[Boost.Asio async_write Boost.Asio 라이브러리의 async_write를 사용하는 방법은 다음과 같음 boost::asio::async_write(s, boost::asio::buffer(data, size), handler); 각 Parameter는 AsyncWirteStream, ConstBufferSequence, WriteHandler로 되어있음 두 번째 Parameter에는 string과 같이 서버 혹은 클라이언트로 보낼 데이터가 들어가게 되며, 공식 Document에서는 Boost.Asio 라이브러리의 buffer를 사용하도록 권장함 Example void write(std::string message) { auto self(shared_from_this()); boost::asio::async_write(_socket, boost::asio::buffer(message), [self, message](boost::system::error_code ec, std::size_t) { if(ec) { std::cerr &lt;&lt; "Error: " &lt;&lt; ec.message() &lt;&lt; "\n"; return; } self-&gt;read(); // 다음 데이터 전송을 대기하기 위한 호출 }); } 예시 코드에서는 Write를 호출하면서 매개변수로 shared_ptr을 넘겨주게 되어있으며 async_write에 매개변수를 넘겨줄 때에는 message를 boost::asio::buffer를 통해 넘겨줌 또한 Handler는 Lambda를 통해서 매개변수를 넘겨주었음 주의할 점 Async 함수 내의 로컬 변수를 handler에서 사용할 가능성이 있다면 메모리 해제를 방지해야 함 예제에서의 message에 대한 관리 공식 문서에는 다음과 같이 설명되어 있음 One or more buffers containing the data to be written. Although the buffers object may be copied as necessary, ownership of the underlying memory blocks is retained by the caller, which must guarantee that they remain valid until the handler is called. 기록할 데이터가 포함된 하나 이상의 버퍼. 버퍼 개체는 필요에 따라 복사할 수 있지만 기본 메모리 블록의 소유권은 호출자가 유지하므로 핸들러가 호출될 때까지 유효하게 유지되어야 합니다. (papago 번역) 쉽게 말하자면 예제에서의 message는 handler가 호출 될 때 까지 유효해야 한다는 것 이와 같이 공식 문서에서도 메모리 관리에 대해 주의해야 한다고 되어있음 해결 방안 스마트 포인터의 사용 void write(std::shared_ptr&lt;std::string&gt; message) { auto self(shared_from_this()); boost::asio::async_write(_socket, boost::asio::buffer(*message), [self, message](boost::system::error_code ec, std::size_t) { if(ec) { std::cerr &lt;&lt; "Error: " &lt;&lt; ec.message() &lt;&lt; "\n"; return; } self-&gt;read(); }); } message 자체를 shared_ptr 로 관리하며 참조 count를 통해 소멸 시점을 늦춤 공식 문서에서 권장하는 방식과 부합하며 비동기적인 처리를 할 수 있는 가장 편하고 안전한 방법이라고 생각하여 사용하게 됨 결론 async_write() 와 같은 비동기 처리 로직이 있다면 스마트 포인터를 적극 활용하여 안전한 비동기 처리를 할 수 있도록 해야 함]]></summary></entry><entry><title type="html">FMOD Unity .gitignore</title><link href="http://0.0.0.0:4000/unity/fmod-gitignore/" rel="alternate" type="text/html" title="FMOD Unity .gitignore" /><published>2024-11-13T00:00:00+00:00</published><updated>2024-11-13T00:00:00+00:00</updated><id>http://0.0.0.0:4000/unity/fmod-gitignore</id><content type="html" xml:base="http://0.0.0.0:4000/unity/fmod-gitignore/"><![CDATA[<blockquote>
  <p>참고 : FMOD .gitignore</p>
</blockquote>

<ul>
  <li>FMOD 프로젝트를 Github에 올리고 관리하기 위해서 추적하는 파일들을 보니 너무 많고 올리지 않아도 되는 파일들이 딸려 들어오는것을 보게 되었다.</li>
  <li>바로 gitignore 파일이 있는지 확인 해 보았고 위 참고 사이트에서 사용한 gitignore 파일을 적용하니 필요한 파일만 올라가게 되었다.</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>### FMOD Unity Integration ###
# Never ignore DLLs in the FMOD subfolder, so make sure that the FMOD-folder is in the correct path.
!/[Aa]ssets/Plugins/FMOD/**/*.dll

# Don't ignore images and gizmos used by FMOD in the Unity Editor
!/[Aa}ssets/Gizmos/FMOD/*
!/[Aa}ssets/Editor Default Resources/FMOD/*

# Ignore the Cache-file since it is updated locally either way
/[Aa]ssets/**/FMODStudioCache.asset
/[Aa]ssets/**/FMODStudioCache.asset.meta
/[Aa]ssets/Plugins/FMOD/Cache.meta
/[Aa]ssets/Plugins/FMOD/Cache/Editor.meta

# Log-files
fmod.log
fmod_editor.log

# FMOD 1.10.x (Legacy)
/Assets/FMODAssets/*
/Assets/FMODStudioCache.meta
</code></pre></div></div>]]></content><author><name>김재우(thejaeu)</name></author><category term="Unity" /><category term="Unity" /><category term="git" /><category term="gitignore" /><summary type="html"><![CDATA[참고 : FMOD .gitignore FMOD 프로젝트를 Github에 올리고 관리하기 위해서 추적하는 파일들을 보니 너무 많고 올리지 않아도 되는 파일들이 딸려 들어오는것을 보게 되었다. 바로 gitignore 파일이 있는지 확인 해 보았고 위 참고 사이트에서 사용한 gitignore 파일을 적용하니 필요한 파일만 올라가게 되었다. ### FMOD Unity Integration ### # Never ignore DLLs in the FMOD subfolder, so make sure that the FMOD-folder is in the correct path. !/[Aa]ssets/Plugins/FMOD/**/*.dll # Don't ignore images and gizmos used by FMOD in the Unity Editor !/[Aa}ssets/Gizmos/FMOD/* !/[Aa}ssets/Editor Default Resources/FMOD/* # Ignore the Cache-file since it is updated locally either way /[Aa]ssets/**/FMODStudioCache.asset /[Aa]ssets/**/FMODStudioCache.asset.meta /[Aa]ssets/Plugins/FMOD/Cache.meta /[Aa]ssets/Plugins/FMOD/Cache/Editor.meta # Log-files fmod.log fmod_editor.log # FMOD 1.10.x (Legacy) /Assets/FMODAssets/* /Assets/FMODStudioCache.meta]]></summary></entry></feed>